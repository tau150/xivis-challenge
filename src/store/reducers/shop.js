import types from 'constants/types';

const initialState = {
  isLoading: false,
  products: [],
  errorMessage: null,
};

const shop = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default shop;
